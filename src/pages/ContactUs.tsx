import React from 'react';

const ContactUs: React.FC = () => {
    return (
        <div className="content">
            <h1>Contact Us</h1> {/* Added an h1 tag for better semantics */}
        </div>
        )
};

export default ContactUs;
